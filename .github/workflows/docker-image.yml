name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest  

    steps:
    - uses: actions/checkout@v3
    - name: Build and push the Docker image
      run: |
       echo "{{ secrets.DOCKERPW }}" | docker login -u "nitin03" --password-stdin
       docker build -t remoteco:$(date +%s) .
